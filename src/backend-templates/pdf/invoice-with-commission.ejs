<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Invoice</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }

        body {
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
        }

        .invoice-container {
            width: 21cm;
            height: 29.7cm;
            background-color: white;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            display: flex;
            flex-direction: column;
            page-break-after: always;
            position: relative;
        }

        .header {
            color: #222;
            border-bottom: 1px solid #D9D9D9;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 18px;
        }

        .header-left {
            display: flex;
            min-height: 60px;
            display: flex;
            align-items: center;
        }

        .header img {
            display: block;
            width: auto;
            height: auto;
            max-width: 100%;
            max-height: 60px;
        }

        .header-line{
            height: 1px;
            width: 20cm;
            padding: 0px 18px;
            margin: 0px 18px 10px 18px;
            background-color: #D9D9D9;
        }

        .clinic-details {
            text-align: right;
            line-height: 1.4;
        }

        .clinic-details p {
            font-size: 13px;
            margin: 2px 0;
        }
        .clinic-details strong {
            font-size: 18px;
            margin: 2px 0;
        }

        .prescription-info-container{
            display: flex;
            justify-content: space-between;
            padding: 10px 18px;
        }
        .prescription-info-container .px-meta-data-right p,
        .prescription-info-container .px-meta-data-left p{
            font-size: 14px;
            color: #444;
        }
        .prescription-info-container .px-meta-data-right p strong,
        .prescription-info-container .px-meta-data-left p strong{
            font-size: 14px;
            color: #222;
        }

        .details-container {
            display: flex;
            justify-content: space-between;
            padding: 10px 18px;
            padding-top: 2px;
            align-items: stretch;
            gap: 12px;
        }

        .details-box {
            width: 50%;
            display: flex;
            flex-direction: column;
            font-size: 12px;
        }

        .patient-details,
        .appointment-details {
            display: flex;
            flex-direction: column;
            gap: 8px;
            padding: 12px;
            border-radius: 6px;
            border: 0.5px solid #D9D9D9;
            flex-grow: 1;
        }

        .details-heading {
            font-size: 13px;
            font-weight: 600;
            margin-bottom: 10px;
            color: #222;
        }

        .patient-details p,
        .appointment-details p {
            display: flex;
            justify-content: space-between;
            gap: 5px;
            margin-block: 0px;
        }

        .patient-details strong,
        .appointment-details strong,
        .age {
            text-align: left;
            font-weight: 400;
            font-size: 12px;
            color: #444;
        }
        .patient-details strong{
            min-width: 65px;
        }
        .patient-details span,
        .appointment-details span,
        .age-value {
            flex-grow: 1;
            text-align: left;
            font-size: 12px;
            color: #222;
        }
        .age-value {
            margin-left: 5px;
        }

        .dob-age {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 30px;
        }

        .table-container {
            margin: 10px 18px;
            margin-top: 2px;
            overflow: hidden;
            border-radius: 10px;
            border: 0.5px solid #CBE3E3;
            /* min-height: 12cm; */
            max-height: 100%;
            position: relative;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            table-layout: fixed;
            border: 0;
        }

        table thead,
        table tr {
            border: 0;
        }

        table th,
        table td {
            font-size: 13px;
            padding: 10px;
            text-align: left;
            vertical-align: top;
            border: 0;
            word-break: break-word;
        }

        table td {
            padding-bottom: 5px;
            padding-top: 5px;
        }
        table tr:first-child td {
            padding-top: 10px;
        }
        table tr:last-child td {
            padding-bottom: 10px;
        }

        table th {
            font-weight: 500;
            color: #444;
            height: 30px;
            font-size: 13px;
            border-bottom: 1px solid #d9d9d9;
        }

        table th:nth-child(1),
        table td:nth-child(1) {
            width: 10%;
        }

        table th:nth-child(2),
        table td:nth-child(2) {
            width: 45%;
        }

        table th:nth-child(3),
        table td:nth-child(3) {
            width: 45%;
            text-align: right;
        }

        .totals-container {
            padding: 15px 18px;
            background-color: white;
            line-height: 2.2;
            font-size: 9px;
        }

        .amount-calc {
            display: flex;
            justify-content: space-between;
            margin-bottom: 5px;
            color: #444;
            font-size: 13px;
        }

        .totals {
            display: flex;
            justify-content: space-between;
            font-size: 14px;
            font-weight: 600;
            border-top: 1px solid #d9d9d9;
            padding-top: 10px;
            color: #222;
        }

        @media print {
            .invoice-container {
                page-break-after: always;
            }
        }
         .logo_url {
            height: 30px;
            object-fit: contain;
        }
        .watermark-container {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 70%;
            opacity: 0.05;
            z-index: 0;
            filter: grayscale(1);
            pointer-events: none;
        }
    </style>
</head>

<body>
    <% 
    const items = scans || [];
    let currentPage = 1;
    let itemsProcessed = 0;
    let availableHeight = 850; // Base available height for table
    
    // Calculate address height impact once before the loop
    const addressParts = [
        patient?.address_line_1,
        patient?.address_line_2,
        patient?.city,
        patient?.postal_code,
        patient?.country
    ].filter(Boolean);

    const fullAddress = addressParts.join(', ');
    if (fullAddress > 85) {
        const extraLines = Math.ceil(fullAddress / 43) - 1;
        availableHeight -= (extraLines * 13) + 10;
    }
    
    // Loop through pages using while loop for dynamic pagination
    while (itemsProcessed < items.length) {
    %>
    <div class="invoice-container">
        <!-- HEADER -->
        <div class="header">
            <div class="header-left">
                <img class="logo_url" src="<%= logo_url || 'https://dtl3fe7urqkwq.cloudfront.net/logo.png' %>" alt="SendScript">
            </div>
            <div class="clinic-details">
                <p><strong>INVOICE</strong></p>
                <p>Powered by SendScript</p>
            </div>
        </div>

        <div class="prescription-info-container">
            <div class="px-meta-data-right">
                <p>Invoice Number: <strong><%= invoice_number %></strong></p>
                <p>Invoice Date: <strong><%= invoice_date %></strong></p>
            </div>
        </div>
        <div class="header-line"></div>

        <div class="details-container">
            <div class="details-box">
                <div class="details-heading">Patient Details</div>
                <div class="patient-details">
                    <p>
                        <strong>Full Name:</strong>
                        <span><%= patient.patient_name %></span>
                    </p>
                    <p class="dob-age">
                        <strong>DOB:</strong>
                        <span>
                          <%= patient.dob %> <%= patient_age %>)
                        </span>
                    </p>
                    <p style="line-height: 16px;">
                        <strong>Address:</strong>
                        <span>
                          <%= (
                            [patient?.address_line_1, patient?.address_line_2, patient?.city, patient?.country, patient?.postal_code]
                              .filter(v => v && v.trim())
                              .join(', ')
                          ) || '-' %>
                        </span>
                      </p>
                      
                      <p>
                        <strong>Contact:</strong>
                        <span><%= patient?.country_code %> <%= patient?.mobile?.trim() || '-' %></span>
                    </p>
                </div>
            </div>

            <div class="details-box">
                <div class="details-heading">Clinic Information</div>
                <div class="appointment-details">
                    <p><strong style="min-width: 100px;">Clinic Name:</strong>
                        <span>
                            <%= patient.site_name %>
                        </span>
                    </p>
                    <p><strong style="min-width: 100px;">Email:</strong> 
                        <span>
                            <%= patient.site_email %>
                        </span>
                    </p>
                    <p><strong style="min-width: 100px;">Telephone:</strong> 
                        <span>
                            <%= patient.site_contact_number %>
                        </span>
                    </p>
                    <p><strong style="min-width: 100px;">Clinic Address:</strong> 
                        <span>
                            <%= [
                              patient.site_address_line_1,
                              patient.site_address_line_2,
                              patient.site_address_line_3,
                              patient.site_city,
                              patient.site_county,
                              patient.site_country,
                              patient.site_postal_code,
                            ].filter(Boolean).join(', ') %>
                        </span>
                    </p>
                </div>
            </div>
        </div>

        <img src="<%= logo_url || 'https://dtl3fe7urqkwq.cloudfront.net/logo.png' %>" class="watermark-container" alt="watermark-container">
        <div class="table-container" data-min-height="<%= Math.round(availableHeight) %>">
            <table>
                <thead>
                    <tr>
                        <th>SL No</th>
                        <th>Scan</th>
                        <th style="text-align: right;">Total Amount</th>
                    </tr>
                </thead>
                <tbody>
                    <% let itemsInThisPage = 0; %>
                    <%
                        let currentHeight = 0;
                        let actualIsLastPage = false;
                    %>
                    <% for (let i = itemsProcessed; i < items.length; i++) { %>
                        <% 
                            // Estimate if this row will fit
                            const item = items[i];
                            let estimatedHeight = 44; // base height

                            // Estimate the height based on scan name + body part length
                            const scanNameText = `${item.scan_name || ''}${item.body_part ? ' - ' + item.body_part : ''}`;
                            if (scanNameText && scanNameText.length > 156) {
                                estimatedHeight += 52;
                            } else if (scanNameText && scanNameText.length > 104) {
                                estimatedHeight += 39;
                            } else if (scanNameText && scanNameText.length > 52) {
                                estimatedHeight += 26;
                            }

                            // Break if adding this row would exceed available height, but only if we already have at least one row in the current page
                            if (currentHeight + estimatedHeight > availableHeight && itemsInThisPage > 0) {
                                break;
                            }

                            currentHeight += estimatedHeight;
                        %>
                        <tr>
                            <td><%= i + 1 %></td>
                            <td><%= item.scan_name %><%= item.body_part ? ' - ' + item.body_part : '' %></td>
                            <td style="text-align: right; align-items: right;">£ <%= item.out_patient_price %></td>
                        </tr>
                        <% 
                            itemsInThisPage++;
                            itemsProcessed++;
                            // Check if this is the last item overall
                            if (i === items.length - 1) {
                                actualIsLastPage = true;
                            }
                        %>
                    <% } %>
                </tbody>
            </table>
        </div>

        <% if (actualIsLastPage) { %>
        <div class="totals-container">
            <div class="amount-calc">
                <div>Gross Amount</div>
                <div>£ <%= Number(grossAmount).toFixed(2) %></div>
            </div>
            <div class="amount-calc">
                <div>Discount</div>
                <div>( - ) £ <%= Number(commissionValue || 0).toFixed(2) %></div>
            </div>
            <div class="totals">
                <div>Net Amount</div>
                <div>£ <%= Number(netAmount || 0).toFixed(2) %></div>
            </div>
        </div>
        <% } else { %>
        <div class="totals-container"></div>
        <% } %>
    </div>
    <% 
        currentPage++;
    } %>
</body>

</html>